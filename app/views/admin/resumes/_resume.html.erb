<div class="panel panel-default">
    <div class="panel-heading">
        <div class="row">
            <div class="col-md-12">
                <h3 class="panel-title">
                    <%= resume.user.email %>
                </h3>
            </div>

        </div>
    </div>
    <div class="panel-body">
        <div class="trix-content">
            <p> <%= simple_format(resume.description) %> </p>
        </div>

        <hr>

        <% if resume.attachment.present? %>
          <i class="fa fa-paperclip"> </i>
          <%= link_to("Download Resume", resume.attachment_url) %>
          <%= button_to "Delete",'#', method: :delete, class: "btn btn-danger pull-right", confirm: "Are you sure that you wish to delete #{resume.description}?" %></td>
        <% else %>
           No Attachment
        <% end %>
    </div>

</div>
