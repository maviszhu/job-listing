<div class='col-md-12'>
  <h2>Jobs</h2>
  <div>
    <%= link_to('Add a job', new_job_path, class: 'btn btn-primary pull-right') %>
  </div>
  <table class='table'>
    <thead>
      <tr>
        <th>#</th>
        <th>Title</th>
        <th>Wages</th>
        <th>Contact</th>
        <th>Updated_at</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @jobs.each do |job| %>
      <tr>
        <td>#</td>
        <td><%= link_to(job.title, job_path(job))%></td>
        <td><%= job.wage_min %> ~ <%= job.wage_max %></td>
        <td><%= job.contact %></td>
        <td><%= job.updated_at %></td>
        <td>
          <% if current_user && current_user == job.user%>
          <%= link_to('Edit', edit_job_path(job), class: 'btn btn-sm btn-default') %>
          <%= link_to('Delete', job_path(job), method: :delete, data: {confirm:'Are you sure?'},class: 'btn btn-sm btn-default') %>
          <% end %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
